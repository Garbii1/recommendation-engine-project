const { defineConfig } = require('@vue/cli-service')
module.exports = {
  // Put your configurations directly here
  // For example, if you had devServer settings:
  // devServer: {
  //   port: 8080
  // },

  // If you have NO specific configurations yet,
  // the file can just be:
  // module.exports = {};
};
